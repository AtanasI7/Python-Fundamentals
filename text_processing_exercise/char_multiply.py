def multiplier():
    data = input().split(' ')
    first = data[0]
    second = data[1]

    multiply_dict = dict(zip(first, second))
    for key, value in multiply_dict.items():
        if key:
            pass


multiplier()
